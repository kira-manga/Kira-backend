# Registered TEST first ordinary epoch seal — controlled source slice

**Source-authored, NOT_COMPILED / NOT_RUN. External intake remains closed.** This is
the first/current-writer ordinary `EPOCH_SEAL` producer only. It consumes the same
live `ComplaintTestNamespaceRegistrationV1`, original process/J/HMAC consumers,
shared publication lanes and normal coordinator used by registration and sealing.
It is not a new registration issuer, a restored/new-process launch path or a
complete terminal lifecycle.

## Cold declaration boundary, not external acceptance

The optional `VersionBoundTestOrdinarySealV1` owner must exist **before full-D,
signed activation and PROJECT**. Its only constructor is the explicitly synthetic
`withHttpFixture` path. There is no deployed/config-property/DI factory, CLI or
ready flag. Absent owner preserves the existing full-D bytes exactly and refuses
`TestRunOrdinarySealV1.begin` before SQL or provider work.

The optional D field commits to environment, run, writer, database and restore
identity; independent last-pre-run restore-horizon and policy declarations; key
retention references; uncertainty/late-arrival bounds; exact deployment principal
and policy mappings; and the existing restricted STS protocol. Catalog PUT/Sign
references must match the retained activation owner. The owner is reference-bound
to the original TEST HMAC consumer and lane registry, not an equal-looking second
secret list. D labels this `CONTROLLED_TEST_ONLY_FIRST_ORDINARY_EPOCH_SEAL` and
`ABSENT_EXTERNAL_VERIFICATION_REQUIRED`.

There is **no authenticated TEST horizon/installed-role intake in this repository**.
J, a begin timestamp, synthetic HTTP identities, declared policy hashes or a
successful local test cannot supply it. Real IAM isolation, installed policies,
backup/restore horizon and retained-key availability remain **EXTERNAL VERIFICATION
REQUIRED**. No signed horizon protocol is invented here.

## Four fixed phases and one original attempt

Every SQL phase uses the original J epoch-seal budget, capped to two seconds per
phase, READ COMMITTED, M(shared) then E(exclusive), global/scoped controls, all22
counters, the registered SEALED run and its genuine paid SYSTEM sealing audit.
There is no callback-defined SQL operation. The earlier run-sealing barrier and
paid audit remain separate prerequisites.

1. **CAPTURE:** acquire an expiring database-time fencing lease; capture the first
   V17 slot as CAPTURED, moving E to E+1. Existing first-slot retries must match the
   same capture/run/provenance. Later rotation sequences, checkpoints and V19 LIVE
   seal-link fields refuse this slice.
2. **PREPARE:** persist canonical `EPOCH_SEAL` ordinal0 in V21, with first range
   1..captured E and empty predecessor, and V14 SEAL_PREPARED. Atomically spend
   exactly the existing SIDECAR storage envelope from run unused/global TEST
   reserve into actual. No ordinary publication or APPLIED seal row is inserted.
3. After genuine PREPARE commit/release, reserve shared routine-J custody and run
   real source-identity → AssumeRole → target-identity STS calls for the exact TEST
   seal key and declared KMS key. The actual TEST terminal codec/HMAC/KMS/AEAD
   creates a candidate. **FREEZE** CAS writes wire/checksum/metadata/retention and
   reloads the immutable durable winner. Its actual commit/release precedes S3.
4. Exact LIST (`maxKeys=2`, no truncation, sibling/version/delete-marker ambiguity)
   conditionally PUTs only when absent, then repeats LIST once, including after
   lost PUT acknowledgment. Exact-version GET verifies raw/SDK facts, frozen bytes,
   digest/checksum, metadata, COMPLIANCE, retention and KMS/AEAD. All native clients
   must actually close and release J before **VERIFY** persists the V14 proof and
   performs the local installation-source observations below in that same phase.

Rollback, UNKNOWN commit, lost afterCommit acknowledgment, unresolved physical or
Spring release, deadline, cancellation, closed registration and lost database
fence cannot authorize a later phase. Native close failure remains sticky and
holds its shared lane; timeout is neither close completion nor revocation. Each
provider sample rechecks original in-process custody, not a renewed attempt.

A new attempt can use the **same still-live registration**, but cannot reuse the
failed invocation or borrow its unreleased resources. It requires a connection-free
caller, actual expiry of the old database lease and its own available shared lane;
failed native owners remain counted, not assumed closed. This slice deliberately
does not release/renew the database lease early. It reloads durable canonical or
frozen intent, does not re-encrypt a frozen winner or double-spend storage, and
preserves first verification bytes/time on exact replay. A verified version missing
from LIST is an error, never permission to PUT a replacement. An absent frozen
object whose requested retention no longer covers the original bounds of the new
attempt also refuses; immutable requested retention is not silently extended.

## Complete local set; explicitly not provider quiescence

Every phase rereads the whole relevant local relation in two bounded keyset passes
(page16, no state/epoch/writer filter that silently drops candidates). Supported
members are genuine primary OWNER_DELETE publications with exact canonical/HMAC,
persisted verification, completed receipt, APPLIED version/hash and recovery-spend
links. Fulfilled and expired-but-retained receipts remain members. Pending,
orphan, foreign, unsupported and mutated history refuses; pending installation
deletion, prior scans or retirement rows also refuse.

The root is SHA-256 of LP32BE UTF-8 fields:

```
kira-complaint-journal-epoch-seal-v1, 1, manifest, writer, ordinaryPrefix,
TEST, run, 1, cutoff, count, then ASCII-key-sorted (key, version, wireSHA256) triples
```

An additional two-pass comparison binds all four supporting row xmin values.
J's retained-version and staging-byte ceilings and the original deadlines bound
the fold. These are **local expected publication-set** checks, not an S3 namespace
inventory, permanent-denial witness, external lineage cut or proof that no late
provider object can arrive. The result name says exactly that limitation.

Retention covers max(ten UTC calendar years from provider creation,
last-pre-run restore horizon +31days, frozen requested retention), with original
remaining-attempt, UTC uncertainty and accepted-late-arrival margins before PUT.
Leap-day handling is calendar arithmetic. Adoption uses the object's immutable
LastModified, not a retry timestamp. Policy and clock bounds are still declarations.

## Local installation-source observations, not disposition application

After the existing history/proof checks, the same VERIFY operation reads **every
reservation in the run twice**, in UUID keyset pages of 16, restarting each pass
and continuing through an empty page. Reservations lead the query; credentials
are left-joined by ID, not used to filter membership. Orphan/wrong-scope credentials,
invalid pairs and pending deletion refuse. Both reservation and credential xmin,
raw state/pair metadata and full membership must agree even when target dispositions
are equal. The actual locked enrollment count must match both complete reads;
the greatest UUID is only an end key, never a temporal watermark/restart cursor.

DELETED targets DELETED; ACTIVE, RETIRED and no-credential RECOVERY_RESERVED target
RETIRED in the pure fold. A preexisting RECOVERY_RESERVED is not pending deletion
or authenticated retirement: it is included when the unchanged first-seal history,
receipt/APPLIED/recovery, epoch/writer/time and source-pair/count checks succeed.
The registered continuation still cannot reconstruct identities or accept aliases.
The connected specimen uses genuine lower primary readback/recovery before real
run sealing, under explicitly synthetic earlier gate comparisons; it does not
establish deployed recovery authority. No reservation or credential is updated by
this observation. Actual terminal application/credential erasure still requires
accepted terminal evidence and PURGING; ordinary retirement instead needs its own
verified event.

The existing installation-root fold and canonical entry-list hashes produce
deterministic plaintext groups of at most 500. A page and one chunk are the largest
retained entry collections. Two local LP32BE UTF-8 SHA-256 folds compare raw source
metadata (`kira-local-test-installation-source-v1`) and plaintext grouping
(`kira-local-test-installation-groups-v1`); these **ephemeral local choices do not
define a future persisted/wire protocol**. Both reads bind run/activation/full-D,
actual database/restore identity, desired generation and the original fence. Actual
registered limits/J byte and retained-version ceilings, the original attempt
deadline and the unchanged two-second SQL phase cap bound all work.

`localInstallationObservation()` returns an existing `TestTerminalProgressV1`
with no denial cuts and exactly two installation reads, only after positive VERIFY
commit, original-holder release and successful completion of the original call.
Unknown commit, lost acknowledgment, deadline/fence/lifetime failure or unresolved
release returns no observation. The existing JSON codec checks canonical bounds.
This is historical local metadata, **not a reusable barrier or terminal authority**;
later preparation must reacquire ownership and revalidate. Nothing is persisted to
the run blob: strict `noTerminal`, all NULL terminal fields, audit/spend/refund,
provider calls and the four existing phases are unchanged.

## Deliberate non-goals and qualification inventory

The run remains SEALED; all final-ordinary/terminal-seal/manifest/denial/purge fields
remain NULL. No gate opens, content autodrains, checkpoint/rotation completion is
issued, catalog is advanced, reserve is refunded, sidecar is deleted, or final
settlement occurs. V14/V17/V19/V21 migrations are unchanged. Multiple writers,
later ordinary seals, terminal seal/manifests/purge and new-process recovery are
not implemented.

Authored connected cases cover empty/nonempty sets, exact replay, complete-history
refusals, phase commit/release faults, lost PUT acknowledgment, provider binding
faults, original lifetime/fence loss and native-close retention. They reuse real
PROJECT/registration/sealing and raw public SDK HTTP fixtures. Nonempty history
inherits explicitly **synthetic earlier open/checkpoint/seal comparisons**; the
expired-receipt variant separately backdates coherent historical comparison times
and recomputes the proof digest. Neither is evidence of real historical acceptance.
Installation-source assertions reuse these empty/enrolled runs and completion
faults; same-transaction corruptions cover omitted/behind-cursor reservations,
same-target state changes, pair/pending and both xmin changes, plus source-time
fence/deadline loss. The failed VERIFY rolls them back. Cheap pure specimens cover
500/501 grouping, mixed/no-credential targets, scope/binding/order/count and actual
J bounds without enlarging enrollment fixtures or changing signed capacity.
Cleanup disables only the V21 immutable trigger inside an owner TEST teardown
transaction to remove owned sidecars; it is never production settlement.

Cold-D, independent LP32, retention and retained-HMAC tests supplement those cases;
existing absent-owner D goldens and narrow codec/STS profile checks are retained.
None was executed in this source slice. Provider policy acceptance, PostgreSQL
physical storage/MVCC sizing, real backup/restore and deployment qualification
remain separate gates. See the frozen review packet for exact NOT_RUN methods.
